[pytest]
markers =
    integration: Integration tests that require more setup and time
    slow: Tests that take longer to run
    performance: Performance benchmarking tests
    
# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output options
addopts = -v --tb=short